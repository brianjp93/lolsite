'use strict';

var _chai = require('chai');

var _getThemeProps = require('./getThemeProps');

var _getThemeProps2 = _interopRequireDefault(_getThemeProps);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('getThemeProps', function () {
  it('should ignore empty theme', function () {
    var props = (0, _getThemeProps2.default)({
      theme: {},
      name: 'MuiFoo',
      props: {}
    });
    _chai.assert.deepEqual(props, {});
  });

  it('should ignore different component', function () {
    var props = (0, _getThemeProps2.default)({
      theme: {
        props: {
          MuiBar: {
            disableRipple: true
          }
        }
      },
      name: 'MuiFoo',
      props: {}
    });
    _chai.assert.deepEqual(props, {});
  });

  it('should return the properties', function () {
    var props = (0, _getThemeProps2.default)({
      theme: {
        props: {
          MuiFoo: {
            disableRipple: true
          }
        }
      },
      name: 'MuiFoo',
      props: {}
    });
    _chai.assert.deepEqual(props, {
      disableRipple: true
    });
  });
});

//# sourceMappingURL=getThemeProps.test.js.map