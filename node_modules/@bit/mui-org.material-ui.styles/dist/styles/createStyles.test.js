'use strict';

var _chai = require('chai');

var _ = require('.');

describe('createStyles', function () {
  it('is the identity function', function () {
    var styles = {};
    _chai.assert.strictEqual((0, _.createStyles)(styles), styles);
  });
});

//# sourceMappingURL=createStyles.test.js.map