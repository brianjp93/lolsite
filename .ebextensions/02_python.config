container_commands:
  01_migrate:
    command: |
      if [ "$ENVNAME" == "lolsite" ]; then
        django-admin.py migrate
      fi
    leader_only: true
  02_createsu:
    command: |
      if [ "$ENVNAME" == "lolsite" ]; then
        source /opt/python/run/venv/bin/activate && python manage.py createsu
      fi
    leader_only: true
  09_collectstatic:
    command: |
      if [ "$ENVNAME" == "lolsite" ]; then
        source /opt/python/run/venv/bin/activate && python manage.py collectstatic --noinput
      fi
    leader_only: true
  10_install_pycurl:
    command: |
      source /opt/python/run/venv/bin/activate && pip uninstall pycurl
      export PYCURL_SSL_LIBRARY=openssl
      source /opt/python/run/venv/bin/activate && pip install pycurl==7.19.3 --no-cache-dir

option_settings:
  aws:elasticbeanstalk:application:environment:
    DJANGO_SETTINGS_MODULE: "lolsite.settings"
  aws:elasticbeanstalk:container:python:
    WSGIPath: lolsite/wsgi.py
